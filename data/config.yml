#log setup
log:
  #can set output to file, others will be outputed to std
  output: std
  name: log.txt
  #level can be: all, debug, info,, warn, error, fatal
  level: all
  enable_line: true

service:
  rpc:
    version: "1.0"
    port: ":50001"

db:
  mysql:
    dsn: root:@tcp(localhost:4000)/push_core
    pool: 10

